# My Spatial Refuge 服务端架构

来源：`common\media\lua\`（B42 解包源码，只读）。

## 1. 启动流程与事件

**require 链**：`MSR_Server.lua:4-25` 先 `00_core/00_MSR`+`helpers/World`，再依次 Shared→RefugeGeneration→Validation→Migration→Decay→UpgradeData→Transaction→Integrity→UpgradeLogic→ReadingSpeed→RoomPersistence→RefugeExpansion→ZombieClear→Death→XPRetention→InventoryAuthority→SpatialWellServer→RefugeGeometry→BasementGeneration。`00_MSR.lua:79-87`：KahluaCompat(K)→Logging(L)→Env→Config→Data→Utils→Difficulty(D)→Events→Validate。`AdminHandler`、`AutomaticCorePickup`、`FertileResonance`、`ZombieCoreDrops` 无人 require，靠目录自动加载(MSR_Server:1-2)。就绪抽象 `MSR.Events.OnServerReady/OnClientReady/OnAnyReady`(Events:65-213) 由 `OnGameStart`(单机/客户端) 与 `OnServerStarted`(专用服/Coop) 驱动。

**PZ 事件（准确行号）**：`OnServerStarted` MSR_Server:1155；`OnClientCommand` :1156、AdminHandler:148、Events:554（总线桥）；`OnPlayerConnect` :1165-1166；`OnCreatePlayer` :1170；`OnTick` :487/1073；`EveryOneMinute` RefugeGeneration:586、ZombieClear:206；`EveryTenMinutes` SpatialWellServer:189；`OnZombieCreate` ZombieCoreDrops:43（守 `isServer() or not isClient()`）；`OnZombieDead`/`OnPlayerMove` AutomaticCorePickup:401/402；`AddXP` XPRetention:705；`OnKeyPressed` BasementGeneration:457；`OnPlayerDeath` Death:287-319；`OnLoad`/`OnPlayerDeath` EffectRegistry:233-234。

**自定义事件**（Events.lua:231-249）：`MSR_PlayerDeath`（Fire Death:255；订阅 MSR_Server:1160、Death:323）、`MSR_PlayerDiedInRefuge`（Death:267→ZombieClear:198）、`MSR_CorpseFound`（Death:151→XPRetention:720）、`MSR_ModDataReady`（Data:104→Integrity:645）；`MSR_CorpseProtected`(Death:107)、`MSR_EssenceCreated`(XPR:362) 只 Fire 无订阅。跨端 `MSR.Events.Server.Register/Fire` 走 `MSR_ServerEvent(Reply)`（Events:554-557；MSR_Server:986-988 忽略）。

**顺序**：`OnServerStart`(:1096-1108) = `InitializeModData`+`TransmitModData`；`OnPlayerConnect`(:1137-1153) = 迁移→`TouchRefugeActivity`；客户端 60 tick 后发 `REQUEST_MODDATA`（Data:152）。

## 2. Refuge 数据模型

存于全局 ModData `ModData.getOrCreate("MySpatialRefuge")`（Config:58、Data:89-91）的 `Refuges`（Config:59）= `[username]=refugeData`，另有 `ReturnPositions`（Data:167）。玩家 ModData 仅备份/legacy：`MSR_ReturnX/Y/Z…`（Data:633-644）、v1 `spatialRefuge_*`。

**字段**（建 Data:390-409；序列化白名单 Data:38-60）：`refugeId/username/centerX/centerY/centerZ/tier/radius/areaOffsetX/areaOffsetY/relicX/relicY/relicZ/upgrades/customizations/createdTime/lastActiveTime/lastExpanded/dataVersion`；运行期加 `roomIds/roomData/roomBounds`(RoomPersistence:92-94)、`isOrphaned/orphanedTime/originalOwner/inheritedFrom/inheritedTime`(Data:523-570)。**refugeId = `"refuge_"..username`**（Data:361/391）。

**坐标尺寸**：槽网格 BASE_X/Y/Z=1000/1000/0、SPACING=50、GRID_SIZE=20（Config:12-16），`col=slot%20`、`slot=row*20+col`（Data:214-246）；`TIERS[0..8].radius=1..9`、`size=radius*2+1`（Config:19-30）。有效几何为 **relic 锚定**：`GetAreaCenter=center±areaOffsetX/Y`、`GetTileBounds=center±radius`、墙取 max+1（RefugeGeometry:32-70）；5 锚点 Center/Up(-1,-1)/Right(1,-1)/Left(-1,1)/Down(1,1)，`GetRelicTarget=center+dx*radius`，扩建 `areaOffsetX-=dx*delta`（:9-15、124-160）；槽键 `"centerX:centerY"`(:164)，最大外扩 18 格(:178)。

**持久化**：`Data.SaveRefugeData`→registry 回写→`ModData.transmit(key)`（Data:178-182、459-497）；写权限由 `canModifyData()`（MP 客户端 false）把关，`_modDataReady` 未置位拒绝新建以防覆盖存档（Data:67-77、344-347）；MP 客户端经 `MODDATA_RESPONSE` 收序列化数据（Data:113-133）。

## 3. 地图生成与扩建

- **RefugeGeneration**（真正建图）：`IsoThumpable.new(cell,square,sprite,isNorth,{})` 建边界墙(:54)与圣物(:245)；圣物强化写死 :259-269（`setMaxHealth(999999)`、`setIsThumpable(false)`、`setBlockAllTheSquare(true)`），ModData `isSacredRelic/refugeId/relicSprite/isProtectedRefugeObject`，`setIsContainer(true)`+`container:setCapacity()`；清树 `square:getTree()/toppleTree()`(:341-346)；入口 `EnsureRefugeStructures`(:363-410)=ReconcileUpper→DeduplicateRelics→ValidateAndRepair。
- **RefugeExpansion**（只编排）：`Prepare` 调 `PlanExpansion`+`IsInsideSlotEnvelope`(:45-80)；`Commit` 存盘后依次 ReconcileUpper、ClearTreesFromArea、ClearZombiesFromArea、BasementGeneration.Generate、RoomPersistence.ApplyCutaway、Integrity.ValidateAndRepair(:83-115)，失败 `MSR.delay(30,…)` 重试(:122-131)。
- **BasementGeneration**：地下室固定 `centerZ-1`，因 BASE_Z=0 **即 z=-1**(:390/422/470)。地板 `square:addFloor("floors_exterior_street_01_17")`(:263)，服务器再 `transmitAddObjectToSquare(floor,-1)`(:266)；缺格 `IsoGridSquare.new(cell,nil,x,y,z)`+`cell:ConnectNewSquare(sq,false)`(:82-83)；楼梯 `IsoObject.new(cell,square,sprite)`(:128)，偏移取 `Config.BASEMENT_STAIRWELLS`(-9/-5、-5/-9，Config:168-189)，zTop=centerZ、zBottom=centerZ-1。
- **BoundaryReconciler + helpers\World**：`reconcileLevel` 按 `buildExpected` 对 `isRefugeBoundary` 对象做保留/收养/删除/新建四路协调，`refugeSlotKey` 防跨槽误删(:105-161)；上层 `centerZ+level`（WALL_HEIGHT=1 只跑 level 0，:163-182）、地下室 centerZ-1(:184-192)。`World.addObject` 按环境分派 `transmitAddObjectToSquare(obj,-1)`（服务器）/`AddSpecialObject`（非服务器），`removeObject` 走 `transmitRemoveItemFromSquare`+pcall(World:319-358)。

## 4. 完整性、边界与迁移

- **Integrity**：`FindRelic`(按 isSacredRelic 或 sprite 名，:17-56)、`ValidateAndRepair`(:473，报 relic/walls/modData 三段)、`DeduplicateRelics`(:544，含容器物品转移 :220)、重绑 sprite（`hasCanonicalSprite` **同时比对 name 与 sprite ID**，专救 B42.20 tiledef ID 变动的旧档，World:283-292）、`ClientSpriteRepair`(:596) 给 MP 客户端做纯视觉修补；z 列表 `{centerZ, centerZ-1}`(:329-335)。
- **时机**：进入 `HandleChunksReady`(MSR_Server:341-488)=`RoomPersistence.RestoreServer`→无 relic 则 EnsureRefugeStructures，否则 ValidateAndRepair+清僵尸；`EveryOneMinute` 周期性(RefugeGeneration:538-588，客户端仅视觉，服务端 60s 冷却、失败后 300s)；扩建后(RefugeExpansion:113)。
- **RoomPersistence**：`refugeData.roomBounds = {[tostring(roomId)]={x,y,w,h,z}}`(:62-95)，扫 radius+1、忽略 getRoomID()=-1；恢复 `getWorld():getMetaGrid():getRoomByID(id)`+`square:setRoomID`(:110-135)，补 `room.addSquare/waterSources/lightSwitches`（`instanceof(obj,"IsoLightSwitch")`，:154-186）；cutaway 用 `setPlayerCutawayFlag/invalidateRenderChunkLevel(2048)`(:193-215)。MP 客户端不能写 ModData，故 `SyncToServer` 发 `SyncClientData`(:244)→服务端 `HandleSyncFromClient`(MSR_Server:966-971)；`RestoreServer`(:278) 生成前调用，`SaveServerOnExit`(:282) **空实现**。⚠ `roomBounds` 不在 `SerializeRefugeData` 白名单，跨端会丢。
- **Migration**（`CURRENT_DATA_VERSION=8`，Config:61，版本存 `refugeData.dataVersion`）：v1→2 `spatialRefuge_*` 转全局表(:44)、v2→3 sprite 换 `myspatialrefuge_0`(:105)、v3→4 补 `upgrades`(:184)、v4→5 `roomIds`(:211)、v5→6 `lastActiveTime`(:233)、v6→7 `customizations`(:253)、v7→8 `areaOffsetX/Y=0`(:273，存盘失败回滚为 7)；`MIGRATIONS[1..7]` 顺序跑(:294-302/359-371)。

## 5. 其他机制

- **Decay**：无事件；按 `lastActiveTime`（回退 `createdTime`）算闲置天数，`DECAY_MIN_DAYS=14`、触发 `REFUGE_DECAY_TRIGGER_PERCENT=95`(=380/400 槽)；分配新槽时 `ShouldAttemptReclaimForAllocation`(Data:378-380)→`ReclaimOldestInactiveRefuge`：先 `SpatialWell.RemoveForRefuge`+`BoundaryReconciler.RemoveSlotBoundaries` 再删数据。
- **ZombieCoreDrops**：`OnZombieCreate`→`ZombRand(100)`→`instanceItem("Base.MagicalCore")`+`zombie:addItemToSpawnAtDeath(item)`；概率 `SandboxVars.MySpatialCore.ZombieCoreDropChance`（默认 30，**仅首次读取**）。
- **AutomaticCorePickup**：服务端权威。`OnZombieDead` 给尸体 30s 归属保护（ModData `MSR_AutomaticCoreOwner/MSR_AutomaticCoreProtectedUntil`），`OnPlayerMove` 触发扫描（≥500ms、半径≤2，受升级 `corePickupRadius` 控制），`getFirstTypeRecurse`+`InventoryAuthority.moveItem` 搬核心；调度靠 `OnTick`(Utils:236)，升级变化经 `MSR.UpgradeEvents.OnLevelChanged`。
- **FertileResonance**：仅 `OnServerReady`，包装 `SFarmingSystem.growPlant`，对庇护所内 `seeded` 植物按 `refugeCropGrowthTimeMultiplier` 提前 `nextGrowing`（下限 1 小时、乘数夹 0.05–1），用 `farming_vegetableconf.calcWater/calcDisease/calcNextTimeFactor`。
- **SpatialWellServer**：`REQUEST_PLACE/MOVE_SPATIAL_WELL` 权威校验（位置、锁定物品、扣料、失败回滚），移动冷却 30s，`EveryTenMinutes` 补水，`MSR_PlayerDeath` 清冷却。
- **XPRetention**：`AddXP` 累计已得 XP（内存表，延迟 10 tick 置 ready），死亡快照(Death:243)→尸体上 `square:AddWorldInventoryItem(instanceItem("Base.MSR_ExperienceEssence"))`；吸收 `XPEssenceAbsorb`→`HandleAbsorbRequest/DoAbsorb`，按 `ESSENCE_RETENTION_PERCENT=75`（沙盒可调）经 `InventoryAuthority.addItem` 返还；精华物品 ModData `msrXpEssence/ownerUsername/xp/createdTs`。
- **Death**：`MSR.Events.Server.On("OnPlayerDeath")` 统一 SP/Coop/MP 转发；10s 去重键 `username:onlineId:x:y:z`；`OnTick` 找尸(≤60 tick)，按 `body:getCharacterOnlineID()` 匹配，`MarkRefugeOrphaned`/`ClearReturnPosition`(:206-213)，清 `spatialRefuge_*`、传送类冷却字段；写 `MSR_CorpseOwner`、`MSR_ProtectedCorpse`。
- **ZombieClear**：`EveryOneMinute`（仅权威端）清 `cell:getZombieList()` 中同 z、半径+3 内僵尸（`removeFromWorld/removeFromSquare`）与 `square:getDeadBodys()`（`removeCorpse`），跳过 `MSR_ProtectedCorpse`；MP 服务端把 `getOnlineID()` 列表经 `CLEAR_ZOMBIES` 下发；玩家死于庇护所内暂停 10s；`centerX<2000 and centerY<2000` 直接跳过。

## 6. 值得注意的 PZ 服务端 API

- 通信/数据：`sendServerCommand/sendClientCommand`、`ModData.getOrCreate(key)/.transmit(key)`、`obj:transmitModData()`、`player:isAccessLevel("admin")`、`getOnlinePlayers()`、`player:getOnlineID()`。
- 地图：`getCell()`、`cell:getGridSquare/getZombieList/ConnectNewSquare`、`IsoGridSquare.new`、`square:getChunk/getObjects/getTree/getDeadBodys/getRoomID/setRoomID/addFloor/removeCorpse`、`RecalcAllWithNeighbours(true)`、`transmitAddObjectToSquare(obj,-1)`、`AddSpecialObject`、`transmitRemoveItemFromSquare`、`AddWorldInventoryItem`。
- 对象：`IsoThumpable.new(cell,square,sprite,isNorth,{})`、`IsoObject.new`、`obj:setSpriteFromName`、`obj:getSprite():getName()/getID()`、`getSprite(name)`、`transmitUpdatedSpriteToClients()`、`setIsContainer/getContainer():setCapacity()/getFirstTypeRecurse()`、`setMaxHealth/setIsThumpable/setBlockAllTheSquare`、`IsoFlagType.solid/solidtrans/solidfloor`、`IsoObjectType.FloorTile`。
- 房间/世界：`getWorld():getMetaGrid():getRoomByID(id)`、`room.addSquare/waterSources/lightSwitches`、`instanceof(obj,"IsoLightSwitch")`、`setPlayerCutawayFlag/invalidateRenderChunkLevel(2048)`。
- 僵尸/尸体：`zombie:getX/Y/Z`、`removeFromWorld/removeFromSquare`、`getInventory/getAttackedBy`、`body:isPlayer/isAnimal/getCharacterOnlineID/getContainer`、`zombie:addItemToSpawnAtDeath(item)`。
- 玩家/物品：`player:getUsername/getX/Y/Z/getInventory/getWornItems/getClothingItem_Back/getPrimaryHandItem/getSecondaryHandItem/getVehicle/teleportTo`、`getXp()`、`PerkFactory.getPerkFromName/PerkList`、`instanceItem`、`container:hasRoomFor/contains`、`player:getEmitter():playSound()`。
- 运行时：`SandboxVars.MySpatialRefuge.*`/`MySpatialCore.*`、`K.time()`、`getCore():getDebug()`、`ZombRand`、`getModInfoByID`。
