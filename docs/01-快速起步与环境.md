<!-- 《Project Zomboid Mod 开发速查手册》拆分章节，内容与单文件版一致 -->

> :book: [返回总目录](README.md)  ·  适用 **Build 42.20.x**  ·  整理于 2026-09

## 0. 五分钟起步（先跑通再说）

1. 游戏装好后，找到用户目录：`C:\Users\<你的用户名>\Zomboid\`
2. 在里面建 `mods\MyFirstMod\`
3. 放进两个东西：`mod.info` + `media\scripts\`（物品/配方脚本）
4. 启动游戏 → 主菜单 **Mods** → 勾选你的 mod → 开新档
5. 想调试就在 Steam 启动项里填 `-debug`，并选 **Alternate launch**

**只做物品和配方的话，到这里你一行代码都不用写。**

---

## 1. 游戏架构：你到底能改什么

Project Zomboid = **Java 引擎 + Lua 游戏逻辑**。

| 层 | 内容 | 能否 mod |
|---|---|---|
| Java 引擎 | 编译成 `zombie/` 下的 `.class` | ❌ 不能直接改，但**部分 API 暴露给 Lua** |
| Lua 逻辑 | 玩法逻辑、UI、事件响应 | ✅ **源码完全公开**，改完重启即生效，无需编译 |
| 脚本 `.txt` | 物品、武器、配方、载具、特质定义 | ✅ 声明式，零编程 |
| 资源 | 贴图 / 模型 / 地图 / 音效 | ✅ 需要美术或官方地图工具 |

**关键机制**：PZ 用 `se.krka.kahlua` 库把 Lua 嵌进 Java。Java 的部分 API 被"声明"进 Lua 的全局命名空间——在 Lua 里调用同名全局函数，实际上就是调 Java 方法。

因此你会看到这种写法：

```lua
-- 这是 Java 的 ArrayList 对象，在 Lua 里用冒号调用其方法
local list = someJavaMethod()
list:get(0)          -- 不是 list.get(0)
list:size()
```

`[社区文档]` PZ-LuaFileStruct

**注意**：Java 的很多原生方法在 Lua 里也能全局调用，但这是开发者手工写进 Lua 命名空间的结果，不是 Lua 语言特性。绕过 Lua 直接调 Java 有性能开销，别放进 `OnTick` 这类每帧回调里。

---

## 2. 资料地图（本机实测可访问性）

| 资源 | 定位 | 可访问 |
|---|---|---|
| [projectzomboid.com/modding/](https://projectzomboid.com/modding/) | **官方 Javadoc**，Java 侧 API 全量。查 `IsoPlayer`、`HandWeapon` 等类的方法签名来这里 | ✅ |
| [pz-wiki-modding.github.io/PZ-API-Docs](https://pz-wiki-modding.github.io/PZ-API-Docs/) | **脚本块文档，页头标注 42.20.4**。每个 `.txt` 脚本块的参数自动生成，**B42 脚本的第一手资料** | ✅ |
| [demiurgequantified.github.io/ProjectZomboidLuaDocs](https://demiurgequantified.github.io/ProjectZomboidLuaDocs/) | Lua 侧 API 文档（42.20.3），社区自动生成，还比较新 | ✅ |
| [FWolfe/Zomboid-Modding-Guide](https://github.com/FWolfe/Zomboid-Modding-Guide) | 459★，最大社区指南。**B41 时代为主**，脚本语法大多仍有效 | ✅ |
| [demiurgeQuantified/PZ-events-guide](https://github.com/demiurgeQuantified/PZ-events-guide) | `Events` / `Hook` 全清单。写机制类 mod 必备 | ✅ |
| [theindiestone.com/forums](https://theindiestone.com/forums/) | 官方论坛。Modding 分区下 **Tutorials & Resources** 最有用 | ✅ |
| [pzwiki.net](https://pzwiki.net/wiki/Modding) | 通常的"第一站"，内容最全 | ❌ **Cloudflare 403**，脚本取不到（浏览器可正常打开） |
| GitHub（`git clone` / `raw.githubusercontent.com`） | **找原版定义的最快路径**——搜抄了原版定义的 mod 仓库 | ✅ 见[附录 B.2](10-数据获取与研究设施.md) |
| [m.namu.moe](https://m.namu.moe/) | 韩语 namu 百科镜像。**PZ 数值极详细**（该页标注 42.20.2） | ✅ 但仅作交叉验证，非官方 |
| [bamboogaming.net/project-zomboid/items](https://www.bamboogaming.net/project-zomboid/items) | B42 物品数据库 | ❌ 持续 429 限流 |
| steamcommunity.com / reddit / archive.org / r.jina.ai | 常见攻略与存档站 | ❌ 全部不可达 |

> 现实提醒：论坛里长期有人抱怨"教程散落各处，没人讲清文件放哪"`[论坛]`。**以 pz-wiki-modding 的脚本文档 + 官方 Javadoc 为准**，视频和二手教程经常是 B41 时代的。
>
> **本机的网络环境比较特殊**（GitHub 被 DNS 劫持、pzwiki 被 Cloudflare 拦、多个攻略站不可达）。踩过的坑、绕行手法和可复制的脚本片段全部整理在 **[附录 B](10-数据获取与研究设施.md)**，下次开发新 mod 前先看那一节。


---

> :book: [返回总目录](README.md)