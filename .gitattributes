# 统一换行符为 LF，避免 Windows/Linux 之间产生无意义的 diff
# （Project Zomboid 对 BOM 敏感，但对 LF/CRLF 两种换行都能正常读取）
* text=auto eol=lf

# 二进制文件不做转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ogg  binary
*.wav  binary
*.bank binary
*.fbx  binary
*.x    binary
*.dll  binary
*.exe  binary
